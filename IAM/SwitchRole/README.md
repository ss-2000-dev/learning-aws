# IAM グループ管理 → スイッチロール移行ガイド

## 背景

当初 IAM Identity Center への移行を検討していたが、**組織側の事情により却下**となった。

- IAM Identity Center は AWS Organizations の**管理アカウント側で組織全体に対して有効化**する必要がある
- 現在利用している AWS アカウントは Organizations の**メンバーアカウント**であり、管理アカウントが組織全体に対して有効化しない限り、メンバーアカウント側でマルチアカウント権限や Permission Set を作成する権限を持てない
- 管理アカウントの操作権限を持たないため、この前提を満たせず断念

代替として、**IAM のクロスアカウントロール（スイッチロール）** を使ったマルチアカウント権限管理に方針転換した。スイッチロールは各メンバーアカウント側の IAM 操作だけで完結するため、Organizations 管理アカウント側の追加設定なしに導入できる。

## このガイドの内容

| ドキュメント | 内容 |
|---|---|
| [01-スイッチロールとは.md](./01-スイッチロールとは.md) | スイッチロールの基本的な仕組み |
| [02-セッション時間.md](./02-セッション時間.md) | スイッチロールのセッション時間の上限・下限、短縮できる限界値 |
| [03-移行手順.md](./03-移行手順.md) | 既存の IAM グループ／ポリシー（CloudFormation管理）からスイッチロールへの移行手順 |
| [04-環境別アクセス制御.md](./04-環境別アクセス制御.md) | 開発／検証／商用の環境別アカウントで、特定ユーザーのみ商用環境へアクセスを許可する方法 |
| [templates/](./templates) | 移行後の CloudFormation サンプル（ベースアカウント側／各環境アカウント側） |
| [templates/switch-role-all-in-one.yaml](./templates/switch-role-all-in-one.yaml) | 上記テンプレート群を1つにまとめた版。`DeploymentTarget` パラメータと `Condition` で、デプロイ先（Staging/Development/Production）ごとに作成するリソースを切り替える。デプロイ手順（コマンド／コンソール操作）は [03-移行手順.md](./03-移行手順.md) の「フェーズ 1・2 の代替」を参照 |

## 前提シナリオ

- 現在: 1つの AWS アカウント（以下「ベースアカウント」）に IAM ユーザーが所属し、`DevelopersTestGroup`（AWS管理ポリシー `PowerUserAccess`）、`InfraTestGroup`（カスタムのインラインポリシー）という2つの IAM グループで権限を管理している（[../../IAMIdentityCenter/sample/before/iam-groups.yaml](../../IAMIdentityCenter/sample/before/iam-groups.yaml) と同じ構成）
- **このベースアカウントは、移行後も検証（Staging）環境として使い続ける。** 新たに用意するのは開発（Dev）・商用（Production）の2アカウントのみで、合計3アカウント構成になる
- 検証環境（ベースアカウント自身）へのアクセスは今まで通り、既存グループの直接権限で行う（スイッチロール不要）。開発・商用環境へは、ベースアカウントのユーザーがスイッチロールで切り替えてアクセスする
- 商用環境のみ、特定ユーザーに限定してアクセスを許可する

## 既存の IAM ユーザー・グループはどうなるか

**既存の IAM ユーザーは削除しない。既存の `DevelopersTestGroup` / `InfraTestGroup` とそこに付いている直接ポリシー（`PowerUserAccess` 等）も削除しない。**

IAM Identity Center への移行（[../../IAMIdentityCenter/](../../IAMIdentityCenter/)）を検討していたときは、最終的に IAM ユーザーを削除して Identity Center ユーザーに置き換える計画だった。しかしスイッチロールへの方針転換後は、ベースアカウントが検証環境を兼ねるため、**既存の IAM ユーザー・グループ・ポリシーはそのまま「検証環境の権限」として使い続ける**。今回追加するのは、その既存グループに「開発・商用アカウントへの AssumeRole 許可」を上乗せする形になる。削除するのではなく、権限を積み増すイメージ。

## 結論だけ知りたい場合

- **スイッチロールのセッション時間は、コンソールの「ロールの切り替え」機能を使う限り 1時間（60分）より短くはできない**。IAM Identity Center の 15分という下限とは異なる（[02-セッション時間.md](./02-セッション時間.md) 参照）
- 商用環境の限定公開は、ベースアカウント側の権限付与を絞るだけでなく、**商用アカウント側のロールの信頼ポリシー（Principal）自体を許可ユーザーだけに絞る**のが本命の対策（[04-環境別アクセス制御.md](./04-環境別アクセス制御.md) 参照）
- **既存の IAM ユーザー・グループ・ポリシーは削除しない**。検証環境の権限としてそのまま存続させる
